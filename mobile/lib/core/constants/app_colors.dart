import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette — golf green
  static const Color primary        = Color(0xFF1B5E20);
  static const Color primaryLight   = Color(0xFF2E7D32);
  static const Color primaryDark    = Color(0xFF003300);

  // Accent — subtle gold
  static const Color gold           = Color(0xFFD4AF37);
  static const Color goldLight      = Color(0xFFFFD966);

  // Backgrounds
  static const Color background     = Color(0xFFF5F5F0);
  static const Color surface        = Color(0xFFFFFFFF);
  static const Color cardBg         = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary    = Color(0xFF1A1A1A);
  static const Color textSecondary  = Color(0xFF6B6B6B);
  static const Color textOnPrimary  = Color(0xFFFFFFFF);

  // Status
  static const Color success        = Color(0xFF388E3C);
  static const Color error          = Color(0xFFD32F2F);
  static const Color warning        = Color(0xFFF57C00);

  // Scorecard
  static const Color birdie         = Color(0xFF1565C0);
  static const Color eagle          = Color(0xFF6A1B9A);
  static const Color bogey          = Color(0xFFD32F2F);
  static const Color par            = Color(0xFF1A1A1A);

  // Divider
  static const Color divider        = Color(0xFFE0E0E0);
}
